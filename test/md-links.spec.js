import { rutaExiste } from "../fn.js";

//const mdLinks = require('../');

describe('rutaExiste', () => {
  it ('deberia retornar false', ()=> {
    rutaExiste(false)
  })
  it ( 'deberia retornar que si existe', ()=> {
    rutaExiste(true)
  })
}

)
describe('mdLinks', () => {

  it('should...', () => {
    console.log('FIX ME!');
  });

});
